= link_to project_path(@project) do
  = fa_icon "arrow-left"
  Project
  
#schema
  %h1.mb-0= @project.name

  %h2.mb-4 Open API Version 3.0.0

  %p.download-link.mb-3
    = link_to project_schema_path(@project, download: true) do
      = fa_icon "download"

  %pre= JSON.pretty_generate(@schema)