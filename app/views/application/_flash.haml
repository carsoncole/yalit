- if flash[:notice] || flash[:alert]
  .row
    .col-md-10.offset-1
      #flash
        - if flash[:notice]
          %p.alert.alert-info
            = flash[:notice]
        - if flash[:alert]
          %p.alert.alert-warning
            = flash[:alert]