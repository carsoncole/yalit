- if flash[:notice] || flash[:alert]
  .row
    .col-md-10.offset-1
      #flash
        - if flash[:notice]
          .alert.alert-info
            = flash[:notice]
        - if flash[:alert]
          .alert.alert-warning
            = flash[:alert]