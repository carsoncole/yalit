{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "bin/rails assets:precompile",
        "test-system": "rails test:system",
        "test-models": "rails test"
      },
      "addons":[
        "heroku-postgresql:in-dyno"
      ],
      "buildpacks": [
        { "url": "https://github.com/heroku/heroku-buildpack-google-chrome" },
        { "url": "heroku/nodejs" }
      ]
    }
  }
}
